import { EmptyState } from "../components/EmptyState";

export default function TradeEntry() {
  return (
    <EmptyState
      title="Trade Entry"
      body="The before/after trade entry flow lands here in Task 28."
    />
  );
}
