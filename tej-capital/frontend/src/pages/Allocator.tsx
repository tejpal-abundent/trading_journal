import { EmptyState } from "../components/EmptyState";

export default function Allocator() {
  return (
    <EmptyState
      title="Allocator view"
      body="The read-only shared tearsheet lands here in Task 29."
    />
  );
}
