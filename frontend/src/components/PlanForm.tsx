// Disabled — replaced by Task 25. Will be deleted in Task 26.
import { Trade } from "../api";
interface _Props {
  trade?: Trade;
  onSaved?: (t: Trade) => void;
  onCancel?: () => void;
}
export default function _Disabled(_props: _Props = {}) { return null; }
